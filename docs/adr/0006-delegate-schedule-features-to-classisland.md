# 将课程显示交由 ClassIsland

ClassHelper 不再实现课程表、节次循环、节假日、教学日历、顶部桌面栏或多屏课程展示。上述功能由 ClassIsland 提供；本项目专注于固定名单、随机点名和快捷启动器。

ClassHelper 不依赖 ClassIsland 才能启动或点名，也不在首版建立插件、进程通信或数据同步。两套软件通过明确的产品边界协作，避免重复配置课程信息和维护两套桌面常驻窗口。
